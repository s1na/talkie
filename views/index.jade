extends layout

block body
  .index-ctrl(ng-controller='IndexCtrl')
    .container
      .row
        .col-md-3.col-md-offset-4
          .login-form
            legend
              آماده شو برای هیجان
            form(method='post', target='/auth', name='login-form', role='form')
              .form-group
                label.sr-only(for='login-form-nickname')
                input#login-form-nickname.form-control(type='text', name='nickname', placeholder='نام مستعار')
              button.btn.btn-primary(type='submit')
                ورود
